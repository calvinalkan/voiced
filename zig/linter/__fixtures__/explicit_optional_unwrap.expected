explicit_optional_unwrap.zig:2:16: explicit_optional_unwrap: `.?` hides an unchecked optional unwrap. Handle null with capture or `orelse`; when null is impossible, use an explicit braced `orelse { unreachable; }` fallback.
        return opt.?;
                   ^

explicit_optional_unwrap.zig:7:20: explicit_optional_unwrap: `.?` hides an unchecked optional unwrap. Handle null with capture or `orelse`; when null is impossible, use an explicit braced `orelse { unreachable; }` fallback.
            return opt.?;
                       ^

explicit_optional_unwrap.zig:14:16: explicit_optional_unwrap: `.?` hides an unchecked optional unwrap. Handle null with capture or `orelse`; when null is impossible, use an explicit braced `orelse { unreachable; }` fallback.
        return opt.?.?;
                   ^

explicit_optional_unwrap.zig:14:18: explicit_optional_unwrap: `.?` hides an unchecked optional unwrap. Handle null with capture or `orelse`; when null is impossible, use an explicit braced `orelse { unreachable; }` fallback.
        return opt.?.?;
                     ^
