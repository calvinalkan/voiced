hidden_control_flow.zig:7:20: hidden_control_flow: `if` has an unbraced statement body. Wrap the body in `{ ... }`; moving it to another line is not sufficient.
        if (condition) return;
                       ^^^^^^

hidden_control_flow.zig:11:20: hidden_control_flow: `if` has an unbraced statement body. Wrap the body in `{ ... }`; moving it to another line is not sufficient.
        if (condition) return error.NotReady;
                       ^^^^^^

hidden_control_flow.zig:16:24: hidden_control_flow: `if` has an unbraced statement body. Wrap the body in `{ ... }`; moving it to another line is not sufficient.
            if (condition) break;
                           ^^^^^

hidden_control_flow.zig:22:24: hidden_control_flow: `if` has an unbraced statement body. Wrap the body in `{ ... }`; moving it to another line is not sufficient.
            if (condition) continue;
                           ^^^^^^^^

hidden_control_flow.zig:27:27: hidden_control_flow: `if` has an unbraced statement body. Wrap the body in `{ ... }`; moving it to another line is not sufficient.
        if (optional) |value| return value;
                              ^^^^^^

hidden_control_flow.zig:33:41: hidden_control_flow: An `if` value branch exits without braces. Wrap that branch in `{ ... }` so the exit is structurally visible.
        const value = if (condition) 1 else return error.NotReady;
                                            ^^^^^^

hidden_control_flow.zig:39:34: hidden_control_flow: An `if` value branch exits without braces. Wrap that branch in `{ ... }` so the exit is structurally visible.
        const value = if (condition) return 1 else 0;
                                     ^^^^^^

hidden_control_flow.zig:46:10: hidden_control_flow: An `if` value branch exits without braces. Wrap that branch in `{ ... }` so the exit is structurally visible.
            (return 1)
             ^^^^^^

hidden_control_flow.zig:52:35: hidden_control_flow: An `orelse` fallback performs statement work or exits without braces. Wrap the fallback in `{ ... }`; a line break alone is not sufficient.
        const value = optional orelse return 0;
                                      ^^^^^^

hidden_control_flow.zig:58:23: hidden_control_flow: A `catch` fallback performs statement work or exits without braces. Wrap the fallback in `{ ... }`; a line break alone is not sufficient.
        operation() catch return;
                          ^^^^^^

hidden_control_flow.zig:62:29: hidden_control_flow: A `catch` fallback performs statement work or exits without braces. Wrap the fallback in `{ ... }`; a line break alone is not sufficient.
        operation() catch |err| return err;
                                ^^^^^^

hidden_control_flow.zig:66:18: hidden_control_flow: `while` has an unbraced body. Wrap the body in `{ ... }`; moving it to another line is not sufficient.
        while (true) break;
                     ^^^^^

hidden_control_flow.zig:70:21: hidden_control_flow: `for` has an unbraced body. Wrap the body in `{ ... }`; this also applies to `inline for`.
        for (items) |_| continue;
                        ^^^^^^^^

hidden_control_flow.zig:76:33: hidden_control_flow: `if` has an unbraced statement body. Wrap the body in `{ ... }`; moving it to another line is not sufficient.
                0 => if (item == 0) continue,
                                    ^^^^^^^^

hidden_control_flow.zig:85:20: hidden_control_flow: `if` has an unbraced statement body. Wrap the body in `{ ... }`; moving it to another line is not sufficient.
        if (condition) startWorker();
                       ^^^^^^^^^^^

hidden_control_flow.zig:90:9: hidden_control_flow: `if` has an unbraced statement body. Wrap the body in `{ ... }`; moving it to another line is not sufficient.
            startWorker();
            ^^^^^^^^^^^

hidden_control_flow.zig:102:12: hidden_control_flow: `if` has an unbraced statement body. Wrap the body in `{ ... }`; moving it to another line is not sufficient.
        } else queueWorker();
               ^^^^^^^^^^^

hidden_control_flow.zig:125:9: hidden_control_flow: `if` has an unbraced statement body. Wrap the body in `{ ... }`; moving it to another line is not sufficient.
            return;
            ^^^^^^

hidden_control_flow.zig:136:9: hidden_control_flow: `while` has an unbraced body. Wrap the body in `{ ... }`; moving it to another line is not sufficient.
            process(item);
            ^^^^^^^

hidden_control_flow.zig:147:9: hidden_control_flow: `for` has an unbraced body. Wrap the body in `{ ... }`; this also applies to `inline for`.
            worker.stop();
            ^^^^^^

hidden_control_flow.zig:157:33: hidden_control_flow: `for` has an unbraced body. Wrap the body in `{ ... }`; this also applies to `inline for`.
        inline for (fields) |field| registerField(field);
                                    ^^^^^^^^^^^^^

hidden_control_flow.zig:168:9: hidden_control_flow: `if` has an unbraced statement body. Wrap the body in `{ ... }`; moving it to another line is not sufficient.
            startWorker();
            ^^^^^^^^^^^

hidden_control_flow.zig:178:23: hidden_control_flow: `if` has an unbraced statement body. Wrap the body in `{ ... }`; moving it to another line is not sufficient.
        defer if (opened) closeWorker();
                          ^^^^^^^^^^^

hidden_control_flow.zig:209:24: hidden_control_flow: A captured, chained, or compound `if` value is compressed onto one line. Put each branch expression on the line after its `if` or `else`; only simple leaf-value branches may remain inline.
        return if (cached) getCachedWorker() else createWorker();
                           ^^^^^^^^^^^^^^^

hidden_control_flow.zig:220:25: hidden_control_flow: A captured, chained, or compound `if` value is compressed onto one line. Put each branch expression on the line after its `if` or `else`; only simple leaf-value branches may remain inline.
        return if (limited) @min(requested, maximum) else requested;
                            ^^^^

hidden_control_flow.zig:231:25: hidden_control_flow: A captured, chained, or compound `if` value is compressed onto one line. Put each branch expression on the line after its `if` or `else`; only simple leaf-value branches may remain inline.
        return if (forward) base + distance else base - distance;
                            ^^^^

hidden_control_flow.zig:242:34: hidden_control_flow: A captured, chained, or compound `if` value is compressed onto one line. Put each branch expression on the line after its `if` or `else`; only simple leaf-value branches may remain inline.
        return if (optional) |value| value else fallback;
                                     ^^^^^

hidden_control_flow.zig:253:24: hidden_control_flow: A captured, chained, or compound `if` value is compressed onto one line. Put each branch expression on the line after its `if` or `else`; only simple leaf-value branches may remain inline.
        return if (failed) .failed else if (ready) .active else .idle;
                           ^

hidden_control_flow.zig:277:28: hidden_control_flow: A compound `orelse` fallback is inline. Start the fallback expression on the line after `orelse`, or use a braced fallback; only simple leaf values may remain inline.
        return optional orelse createWorker();
                               ^^^^^^^^^^^^

hidden_control_flow.zig:286:28: hidden_control_flow: A compound `orelse` fallback is inline. Start the fallback expression on the line after `orelse`, or use a braced fallback; only simple leaf values may remain inline.
        return optional orelse createValue() + offset;
                               ^^^^^^^^^^^

hidden_control_flow.zig:296:9: hidden_control_flow: An `orelse` fallback performs statement work or exits without braces. Wrap the fallback in `{ ... }`; a line break alone is not sufficient.
            return error.MissingWorker;
            ^^^^^^

hidden_control_flow.zig:303:10: hidden_control_flow: An `orelse` fallback performs statement work or exits without braces. Wrap the fallback in `{ ... }`; a line break alone is not sufficient.
            (return error.MissingWorker);
             ^^^^^^

hidden_control_flow.zig:318:9: hidden_control_flow: An `orelse` fallback performs statement work or exits without braces. Wrap the fallback in `{ ... }`; a line break alone is not sufficient.
            unreachable;
            ^^^^^^^^^^^

hidden_control_flow.zig:329:9: hidden_control_flow: An `orelse` fallback performs statement work or exits without braces. Wrap the fallback in `{ ... }`; a line break alone is not sufficient.
            reportUnavailable();
            ^^^^^^^^^^^^^^^^^

hidden_control_flow.zig:348:31: hidden_control_flow: A compound `catch` fallback is inline. Start the fallback expression on the line after `catch`, or use a braced fallback; only simple leaf values may remain inline.
        return loadWorker() catch recoverWorker();
                                  ^^^^^^^^^^^^^

hidden_control_flow.zig:357:37: hidden_control_flow: A compound `catch` fallback is inline. Start the fallback expression on the line after `catch`, or use a braced fallback; only simple leaf values may remain inline.
        return loadWorker() catch |err| recoverWorkerFrom(err);
                                        ^^^^^^^^^^^^^^^^^

hidden_control_flow.zig:367:9: hidden_control_flow: A `catch` fallback performs statement work or exits without braces. Wrap the fallback in `{ ... }`; a line break alone is not sufficient.
            return err;
            ^^^^^^

hidden_control_flow.zig:382:9: hidden_control_flow: A `catch` fallback performs statement work or exits without braces. Wrap the fallback in `{ ... }`; a line break alone is not sufficient.
            unreachable;
            ^^^^^^^^^^^

hidden_control_flow.zig:393:9: hidden_control_flow: A `catch` fallback performs statement work or exits without braces. Wrap the fallback in `{ ... }`; a line break alone is not sufficient.
            logFailure(err);
            ^^^^^^^^^^

hidden_control_flow.zig:405:15: hidden_control_flow: `try` is nested inside another expression on the same line, hiding its implicit error return. Start `try` on a new line or assign its result before the outer expression.
        configure(try loadConfig());
                  ^^^

hidden_control_flow.zig:428:27: hidden_control_flow: `try` is nested inside another expression on the same line, hiding its implicit error return. Start `try` on a new line or assign its result before the outer expression.
        configure(.{ .value = try loadValue() });
                              ^^^

hidden_control_flow.zig:459:20: hidden_control_flow: An `errdefer` error capture has an unbraced handler. Wrap the handler in `{ ... }`; plain `errdefer cleanup();` remains allowed.
        errdefer |err| logFailure(err);
                       ^^^^^^^^^^
