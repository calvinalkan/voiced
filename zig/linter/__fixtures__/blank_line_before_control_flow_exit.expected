blank_line_before_control_flow_exit.zig:3:5: blank_line_before_control_flow_exit: `return` follows another statement in the same block without an empty line. Insert an empty line before `return`; if comments describe this exit, put the empty line before those comments. No empty line is required when no statement precedes `return` in its block.
        return result;
        ^^^^^^

blank_line_before_control_flow_exit.zig:9:9: blank_line_before_control_flow_exit: `break` follows another statement in the same block without an empty line. Insert an empty line before `break`; if comments describe this exit, put the empty line before those comments. No empty line is required when no statement precedes `break` in its block.
            break;
            ^^^^^

blank_line_before_control_flow_exit.zig:16:9: blank_line_before_control_flow_exit: `continue` follows another statement in the same block without an empty line. Insert an empty line before `continue`; if comments describe this exit, put the empty line before those comments. No empty line is required when no statement precedes `continue` in its block.
            continue;
            ^^^^^^^^

blank_line_before_control_flow_exit.zig:22:5: blank_line_before_control_flow_exit: `unreachable` follows another statement in the same block without an empty line. Insert an empty line before `unreachable`; if comments describe this exit, put the empty line before those comments. No empty line is required when no statement precedes `unreachable` in its block.
        unreachable;
        ^^^^^^^^^^^

blank_line_before_control_flow_exit.zig:28:9: blank_line_before_control_flow_exit: `break` follows another statement in the same block without an empty line. Insert an empty line before `break`; if comments describe this exit, put the empty line before those comments. No empty line is required when no statement precedes `break` in its block.
            break :result value;
            ^^^^^

blank_line_before_control_flow_exit.zig:36:13: blank_line_before_control_flow_exit: `return` follows another statement in the same block without an empty line. Insert an empty line before `return`; if comments describe this exit, put the empty line before those comments. No empty line is required when no statement precedes `return` in its block.
                return;
                ^^^^^^

blank_line_before_control_flow_exit.zig:45:5: blank_line_before_control_flow_exit: `return` follows another statement in the same block without an empty line. Insert an empty line before `return`; if comments describe this exit, put the empty line before those comments. No empty line is required when no statement precedes `return` in its block.
        return;
        ^^^^^^
